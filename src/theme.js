import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#fff",
      light: "#E10000",
    },
    secondary: {
      main: "#DEE119",
    },
    otherColor: {
      main: "#fff",
    },
  },
});
